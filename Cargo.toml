[package]
description = "ternary-quantize - SuperInstance ecosystem crate"
repository = "https://github.com/SuperInstance/ternary-quantize"
name = "ternary-quantize"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
